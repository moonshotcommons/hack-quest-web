import { Metadata } from 'next';
import Instructor from '../components';

export const metadata: Metadata = {
  title: 'HackQuest Instructor',
  alternates: {
    canonical: 'https://www.hackquest.io/instructor'
  }
};

export default Instructor;
