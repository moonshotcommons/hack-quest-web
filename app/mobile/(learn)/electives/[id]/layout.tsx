import BaseLayout from '@/components/Layout/BaseLayout';

const ElectiveDetailDetailLayout = ({
  children
}: {
  children: React.ReactNode;
}) => {
  return <BaseLayout>{children}</BaseLayout>;
};

export default ElectiveDetailDetailLayout;
