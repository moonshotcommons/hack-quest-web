import BaseLayout from '@/components/Layout/BaseLayout';

const LearningTrackDetailLayout = ({
  children
}: {
  children: React.ReactNode;
}) => {
  return <BaseLayout>{children}</BaseLayout>;
};

export default LearningTrackDetailLayout;
